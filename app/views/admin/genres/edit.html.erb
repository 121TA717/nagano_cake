<h1>ジャンル編集画面</h1>

<%= form_with model: @genre, url: edit_admin_genre_path do |f| %>
  <div class="field">
    <%= f.label 'ジャンル名' %>
    <%= f.select :genres, autofocus: true, autocomplete: "ジャンル名" %>
    <%= link_to '変更を保存', admin_genre_path, class: "btn btn-success" %>
  </div>
<% end %>